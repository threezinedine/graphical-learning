{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build",
			"type": "shell",
			"command": "uv",
			"args": ["run", "helper.py", "build"],
			"options": {
				"cwd": "${workspaceFolder}"
			}
		}
	]
}
